import globalStyles from '../../global/globalStyle';

export default {
  title: {
    ...globalStyles.title,
    marginBottom: 15
  },
  chartBottomHeader: {
    fontWeight: 'bold',
    textAlign: 'center'
  }
};
