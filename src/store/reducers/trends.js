const INITIAL_STATE = {
  trends: []
};

const trendsReducer = (state = INITIAL_STATE, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default trendsReducer;
